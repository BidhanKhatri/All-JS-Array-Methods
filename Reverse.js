// reverse() method is used to reverse the elements of an array

// Syntax: array.reverse();

const array = [1, 2, 3, 4, 5];
const names = ["ram", "shyam", "hari", "geta"];

console.log(array.reverse()); // [5, 4, 3, 2, 1]
console.log(names.reverse()); // ["geta", "hari", "shyam", "ram"]
